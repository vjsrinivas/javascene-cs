import java.awt.*;
import java.applet.*;

class Speaker
{
   public static void drawSpeaker(Graphics g, int sx, int sy)
   {
      g.setColor(new Color(12,12,12));
      g.fillRect(250, 250, sx, sy);
      
   }
}